name: CI

on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true
    env:
      RESULTS_KEY: dotnet-8-net8-0

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore LiteDB.sln

      - name: Build
        run: dotnet build LiteDB.sln --configuration Release --no-restore /p:DefineConstants=TESTING

      - name: Test (with hang diagnostics)
        timeout-minutes: 8
        env:
          VSTEST_TESTHOST_HANG_DUMP_TYPE: full
          VSTEST_TESTHOST_HANG_DUMP_FOLDER: ${{ github.workspace }}/hangdumps
          DOTNET_SKIP_FIRST_TIME_EXPERIENCE: 1
          DOTNET_NOLOGO: 1
        run: >-
          dotnet test LiteDB.sln
          --configuration Release
          --no-build
          --verbosity normal
          --settings tests.ci.runsettings
          --logger "trx;LogFileName=TestResults.trx"
          --logger "console;verbosity=detailed"
          --blame-hang --blame-hang-timeout 00:02:00
          --diag "TestHost.diag.log;tracelevel=verbose"
          /p:DefineConstants=TESTING

      - name: Collect test results
        if: always()
        run: |
          set -euxo pipefail
          results_dir="artifacts/${RESULTS_KEY}"
          mkdir -p "${results_dir}"

          shopt -s globstar nullglob
          files=(**/TestResults/*.trx)
          if [ ${#files[@]} -gt 0 ]; then
            cp "${files[@]}" "${results_dir}/"
          fi

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-${{ env.RESULTS_KEY }}
          path: artifacts/${{ env.RESULTS_KEY }}
          if-no-files-found: ignore

      - name: Upload hang dumps (if any)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: hangdumps
          path: |
            hangdumps
            **/TestResults/**/*.dmp
          if-no-files-found: ignore
