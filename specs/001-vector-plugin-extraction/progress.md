# Progress - Vector Plugin Extraction

## 2025-11-16
- Completed **T001** (`docs/vector-plugin-isolation.md`). Added the migration guide summarizing plugin optionality, prerelease migration paths, missing-plugin diagnostics, CI/test expectations, and troubleshooting so users understand how to install LiteDB.Vector and migrate existing databases.
- Completed **T002** (`scripts/verify-vector-clean.ps1`). Implemented the ripgrep-based guard that scans LiteDB/ for "Vector" references, supports repo-relative allowlists, and exits with an error when vector strings remain outside approved extension points.
- Completed **T003** (`LiteDB/Plugins/Indexing/CustomIndexStrategyDescriptor.cs`, `LiteDB/Plugins/ILitePlugin.cs`, `LiteDB/Plugins/EnsureIndexContext.cs`, `LiteDB/Plugins/DefaultPluginContext.cs`, `LiteDB/Client/Database/LiteDatabaseServices.cs`, `LiteDB.Vector/*`). Generalized the vector strategy descriptor into `CustomIndexStrategyDescriptor`, added `ICustomIndexStrategyRegistry`, renamed plugin context accessors to `CustomIndexes`, updated LiteDB.Vector registrations/tests/scripts/docs, and removed the old vector-specific contract so future plugins can register their own index strategies through the shared registry.
- Completed **T003a** (`LiteDB/Plugins/Indexing/IPluginIndexMetadataRegistry.cs`). Defined the contract types for plugin-owned index metadata serialization, including the registry interface, descriptor, and payload container so future steps can decouple metadata persistence from strategy logic.
- Completed **T004a** (`LiteDB/Plugins/Storage/IPageTypeRegistry.cs`, `LiteDB/Plugins/Storage/PageTypeRegistry.cs`, `LiteDB/Plugins/ILitePlugin.cs`, `LiteDB/Plugins/DefaultPluginContext.cs`, `LiteDB/Engine/Pages/PageFactoryRegistry.cs`, `LiteDB.Vector/VectorSearchPlugin.cs`, docs/tests). Replaced the old opaque page factory registry with the new page-type registry/descriptor/context, added numeric page codes, updated engine plumbing to route buffers through `PageConstructionContext`, and refreshed LiteDB.Vector plus docs/tests to consume the new registration flow.
- Completed **T004** (`LiteDB/Plugins/Bson/CustomBsonTypeDescriptor.cs`, `LiteDB/Plugins/Bson/CustomBsonTypeRegistry.cs`, `LiteDB/Document/BsonType.cs`, `LiteDB/Document/Bson/BsonTypeRegistry.cs`, `LiteDB/Plugins/ILitePlugin.cs`, `LiteDB/Plugins/DefaultPluginContext.cs`). Renamed the BSON registry surface to `ICustomBsonTypeRegistry`, introduced the `CustomBsonTypeDescriptor` delegates, updated the default registry implementation, and rewired core consumers plus docs so plugin-defined BSON handlers register through the new descriptor.
- Completed **T004b–T004d** (`LiteDB/Plugins/Query/ISqlFunctionRegistry.cs`, `LiteDB/Plugins/Query/SqlFunctionRegistry.cs`, `LiteDB/Plugins/Query/IQueryOperatorRegistry.cs`, `LiteDB/Plugins/Query/QueryOperatorRegistry.cs`, `LiteDB/Plugins/Query/IQueryCostModelRegistry.cs`, `LiteDB/Plugins/Query/QueryCostModelRegistry.cs`). Added the SQL function/operator/cost registries with their descriptor types and default implementations so upcoming phases can expose them through the plugin context and planner.
- Completed **T005** (`LiteDB/Plugins/PluginDiagnosticPolicy.cs`, `LiteDB/Plugins/ILitePlugin.cs`, `LiteDB/Plugins/DefaultPluginContext.cs`). Introduced `IPluginDiagnosticPolicy` plus the default implementation scaffold, added the missing-behavior enum, and wired ILitePluginContext with setter/accessor support so plugins can declare how LiteDB should react when plugin assets are accessed without the plugin present.
- Completed **T005a** (`LiteDB/Plugins/ReservedCodeRanges.cs`, `LiteDB/Plugins/Bson/CustomBsonTypeRegistry.cs`, `LiteDB/Plugins/Storage/PageTypeRegistry.cs`, `LiteDB/Plugins/DefaultPluginContext.cs`, `LiteDB/Plugins/Query/*Registry.cs`). Added shared helpers enforcing LiteDB.Vector's reserved code ranges and updated all registries to detect duplicate registrations, throwing `InvalidOperationException` with conflicting plugin details before state is mutated.
- Completed **T006** (`LiteDB/Plugins/ILitePlugin.cs`, `LiteDB/Plugins/DefaultPluginContext.cs`, `LiteDB/Client/Database/LiteDatabaseServices.cs`). Verified the plugin context now instantiates/exposes the diagnostic policy plus SQL function/operator/cost registries and that `LiteDatabaseServices` surfaces these properties so consumers and plugins can resolve them at runtime without reflection.
- Investigated **T006a** and confirmed the LiteDB.Vector plugin still consumes internal engine types such as `BasePage`, `PageBuffer`, `Snapshot`, and `CollectionPage` (see `LiteDB.Vector/VectorIndexStrategy.cs`, `LiteDB.Vector/Engine/Pages/VectorIndexPage.cs`, `LiteDB.Vector/Engine/VectorIndexService.cs`). Removing the `InternalsVisibleTo` declarations currently breaks the build, so the entries remain until the upcoming custom index/metadata/page registry work replaces those dependencies per the plan in `specs/001-resolve-vector-findings/progress.md` (T029). As a mitigation we will add verification steps (API diff/reflection scans plus `scripts/verify-vector-clean.ps1` extensions) proving LiteDB core exposes, ideally little to no, vector APIs while InternalsVisibleTo stays in place.
- Completed **T006a** (`scripts/verify-vector-api.ps1`, `scripts/verify-vector-api.allowlist`, `docs/vector-plugin-isolation.md`). Added the reflection-based guard that builds LiteDB, inspects each target framework's `LiteDB.dll`, and fails if new public types/members containing "`Vector`" appear outside the allow list. Seeded the allow list with the current intentional surface (`LiteDB.Query` vector helpers, `LiteDB.BsonValue` conversions, `LiteDB.BsonType/BsonExpressionType` enum values, and the temporary `VectorIndexMetadataSerializer` bridge) so we can track and shrink it as Phase 3 removes the APIs. Wired the guard into the migration guide's verification list and captured proof-of-work by running `pwsh -File scripts/verify-vector-api.ps1 -NoBuild` (passes: 30 allowed exposures, 0 violations) alongside the existing `scripts/verify-vector-clean.ps1` grep check.
- Completed **T007** (`LiteDB/Client/Database/Collections/Index.cs`, `LiteDB/Client/Database/LiteCollection.cs`, `LiteDB/Client/Database/LiteDatabase.cs`, `LiteDB/Client/Database/LiteDatabaseServices.cs`, `LiteDB/Plugins/ILitePlugin.cs`, `LiteDB/Plugins/DefaultPluginContext.cs`, `LiteDB.Tests/Plugins/PluginInfrastructureTests.cs`, `docs/plugins/plugin-development.md`). Removed the now-obsolete `IndexInterceptors` plumbing and the `TryExecuteIndexInterceptors` helper from `LiteCollection`, updated the plugin context/services surface to rely on the custom index registries instead, refreshed docs/tests to point at `RegisterCustomIndexStrategy`, and validated the solution with `dotnet build LiteDB.sln -c Release`.
- Completed **T008** (`LiteDB/Engine/ILiteEngine.cs`, `LiteDB/Engine/Engine/Index.cs`, `LiteDB/Engine/Engine/Rebuild.cs`, `LiteDB/Client/Shared/SharedEngine.cs`, `LiteDB.Vector/Extensions/LiteCollectionVectorExtensions.cs`, `LiteDB.Tests/Engine/Plugins/PluginAbsentTests.cs`). Replaced the public `EnsureVectorIndex` API with the neutral `EnsureCustomIndex` hook, updated shared engine/rebuild flows plus plugin extensions to call the new strategy-based entry point, refreshed diagnostics/messages to emit generic plugin metadata, and re-ran `dotnet build LiteDB.sln -c Release` to confirm the solution stays green.
- Completed **T009** (`LiteDB/Plugins/ILitePlugin.cs`, `LiteDB/Plugins/DefaultPluginContext.cs`, `LiteDB/Client/Database/LiteDatabaseServices.cs`, `LiteDB.Vector/Extensions/LiteCollectionVectorExtensions.cs`, `LiteDB.Vector/Utils/VectorExtensionHelpers.cs`, `LiteDB.Vector/VectorSearchPlugin.cs`, `LiteDB.Vector/Utils/VectorMetadataSerializer.cs`). Surfaced the plugin index metadata registry through `ILitePluginContext`/`LiteDatabaseServices`, wired the default context and vector plugin to register the serializer descriptor (`indexKind = "vector.hnsw"`), and rebuilt the vector collection extensions so all EnsureIndex overloads require plugin registries (strategy + metadata) and materialize their options via the metadata serializer before invoking the custom index delegate. Validated with `dotnet build LiteDB.sln -c Release` (existing nullable/telemetry warnings only).
- Completed **T010** (`LiteDB.Vector/Utils/VectorCompatibility.cs`, `LiteDB/Plugins/ReservedCodeRanges.cs`, `LiteDB/Plugins/PluginExceptionHelper.cs`, `LiteDB/Engine/Engine/Index.cs`, `LiteDB/Engine/Engine/Rebuild.cs`, `LiteDB/Engine/Pages/PageFactoryRegistry.cs`, `LiteDB.Vector/Extensions/*`, `LiteDB.Vector/Extensions/QueryableExtensions.cs`, `LiteDB.Vector/VectorSearchPlugin.cs`, `LiteDB.Tests/Engine/*`). Moved the compatibility helper into the plugin assembly, introduced general-purpose plugin exception helpers/constants, rewired core callers (custom index diagnostics, rebuild, page factory) to emit neutral plugin-required messages, and updated the vector extensions/tests to reference the new plugin-scoped helper exclusively. `dotnet build LiteDB.sln -c Release` succeeds with only the pre-existing nullable warnings in LiteDB.Vector.
- Completed **T011** (`LiteDB.Tests/Client/VectorOptionalityTests.cs`, `scripts/verify-vector-api.allowlist`). Added a dedicated test suite that reflects over LiteDB’s public API to ensure every `Vector*` symbol matches the curated allow list and that LiteDB.Vector continues to expose the vector extension/plug-in types. Tests reuse the canonical allow-list file so CI and reflective guard stay in sync. Verified with `dotnet test LiteDB.Tests/LiteDB.Tests.csproj -c Release --filter VectorOptionalityTests`.

## 2025-11-17
- Completed **T012** (`LiteDB/Engine/Pages/CollectionPage.cs`, `LiteDB.Vector/*`, `LiteDB/Engine/SystemCollections/SysIndexes.cs`, `LiteDB/Engine/Services/SnapShot.cs`, `LiteDB/Engine/FileReader/FileReaderV8.cs`, `LiteDB.Vector.Tests/*`, `LiteDB.Tests/Engine/*`, `scripts/vector/MigrationHelpers.cs`). Replaced the vector-specific metadata dictionary with a plugin-agnostic metadata bag that persists `{pluginIdLength|0x80}{pluginId}{payloadLength}{payload}` entries, added compatibility handling for legacy slots, introduced `InsertPluginIndex` plus new enumeration/query helpers, and updated the vector plugin (strategy, extension helpers, serializer) along with engine consumers/tests/scripts to depend on the plugin metadata registry and plugin IDs instead of `VectorIndexMetadataSerializer`. Verified with `dotnet build LiteDB.sln -c Release` (pre-existing nullable warnings only).
- Completed **T013** (`LiteDB/Engine/Engine/Rebuild.cs`, `LiteDB/Engine/FileReader/IndexInfo.cs`, `LiteDB/Engine/FileReader/FileReaderV8.cs`). Extended the file reader to surface plugin IDs/payloads per index, overhauled `RebuildContent` to route custom index recreation through `IPluginIndexMetadataRegistry` + `ICustomIndexStrategyRegistry`, and added helpers that deserialize metadata into `_pluginMetadata` envelopes, resolve strategy kinds, invoke optional rebuild delegates, and fall back to the legacy vector serializer when no plugin payload exists. `dotnet build LiteDB.sln -c Release` succeeds (only existing warnings remain).
- Completed **T013a** (`LiteDB/Engine/FileReader/FileReaderV8.cs`, `LiteDB/Engine/FileReader/IndexInfo.cs`, `LiteDB/Engine/Engine/Rebuild.cs`, `LiteDB/Utils/LiteException.cs`, `LiteDB/Plugins/PluginExceptionHelper.cs`, `LiteDB.Vector/Utils/VectorCompatibility.cs`). `FileReaderV8` now consults `IPluginIndexMetadataRegistry` while loading collection pages, deserializes plugin metadata into `IndexInfo.PluginMetadataDocument`, and throws a standardized `LiteException` (`LITE2002`) when the owning plugin/serializer is missing so rebuilds abort safely. Added the `PLUGIN_REQUIRED` error code, updated plugin exception helpers/vector compatibility to use it, and taught `Rebuild` to reuse the pre-deserialized metadata. Validated with `dotnet build LiteDB.sln -c Release` (only longstanding warnings remain).
- Completed **T014** (`LiteDB/Engine/Services/SnapShot.cs`, `LiteDB/Engine/Engine/Index.cs`, `LiteDB/Plugins/PluginDiagnosticPolicy.cs`). Snapshot creation now inspects plugin-owned metadata, consults `IPluginDiagnosticPolicy`, and either refuses database startup or logs a single warning per plugin before allowing non-vector operations; index/rebuild paths resolve plugin IDs from metadata/options and route all missing-plugin diagnostics through the policy so errors consistently surface as `LITE2002` with contextual payloads. `dotnet build LiteDB.sln -c Release` succeeds (existing warnings only).
- Completed **T015** (`LiteDB.Vector/VectorSearchPlugin.cs`, `LiteDB.Vector/Utils/VectorPluginDiagnosticPolicy.cs`). Introduced a vector-specific diagnostic policy (`PluginMissingBehavior.RefuseOperations`) that formats LITE2002 guidance and registered it during plugin initialization alongside the `vector.hnsw` metadata descriptor and `0xE0` page factory using the reserved plugin ID constant. With the policy installed, core enforcement paths emit plugin-branded diagnostics, and the plugin registration logic now relies solely on the reserved identifier constants. Verified via `dotnet build LiteDB.sln -c Release` (pre-existing warnings only).
- Completed **T016** (`LiteDB.Tests/Engine/VectorMetadataCompatibilityTests.cs`). Added integration tests that seed a database with vector metadata, then reopen it with and without the plugin to verify (a) non-vector operations remain available, (b) vector operations throw `LiteException.PLUGIN_REQUIRED (LITE2002)` when the plugin is missing, and (c) the snapshot warning cache emits exactly one warning per plugin ID. When the plugin is installed, the tests confirm vector operations succeed and no warnings are recorded. `dotnet build LiteDB.sln -c Release` remains green with the existing warnings only.
- Completed **T016a** (`LiteDB.Tests/Engine/BehaviorMatrixIntegrationTests.cs`). Implemented behavior-matrix coverage by seeding vector metadata and running every scenario (safe/unsafe prerelease artifacts, GA vector data with/without plugin, compaction/shrink flows) to assert the expected warning cadence and `LITE2002` enforcement; compaction now throws when the plugin is absent and succeeds once the plugin is registered. `dotnet build LiteDB.sln -c Release` stays green aside from longstanding warnings.

## 2025-11-17
- Completed **T017** (expression/query extensibility). Removed vector-specific entries from LiteDB/Document/Expression/Parser/BsonExpressionType.cs, taught the tokenizer/parser/BsonExpression infrastructure to source custom operators via IQueryOperatorRegistry, and added CustomExpressionName metadata so plugins can recognize their tokens. Updated LiteDB.Vector (planner + plugin) and associated tests to consume the new hooks, and aligned PageType.VectorIndex with the reserved plugin code so page factories stay plugin-driven.
- Completed **T018** (`LiteDB/Utils/Extensions/BufferSliceExtensions.cs`). Removed the `BsonType.Vector` switch path for index keys, routed read/write helpers through the plugin BSON registry, and added fallbacks that use `LiteDatabaseServices.Default.Context` when optional plugins are absent so custom BSON encodings can be isolated to LiteDB.Vector.
- Validation: `dotnet test LiteDB.Tests/LiteDB.Tests.csproj --filter BsonVector_Tests` *(fails to compile)* because `LiteDatabaseOptions` is undefined for the netfx targets referenced by `LiteDB.Tests/BsonValue/BsonVector_Tests.cs`; failure pre-dates this change.

